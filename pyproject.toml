[project]
name = "vega-level-sensor"
version = "0.1.0"
description = "Doover application for monitoring water level with Vega level sensors"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pydoover>=1.4.2",
    "transitions>=0.9.2",
]

[project.scripts]
doover-app-run = "vega_level_sensor:main"
export-config = "vega_level_sensor.app_config:export"
export-ui = "vega_level_sensor.app_ui:export"
export-config-dashboard = "farm_water_dashboard.app_config:export"
export-ui-dashboard = "farm_water_dashboard.app_ui:export"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/vega_level_sensor", "src/farm_water_dashboard"]

[dependency-groups]
dev = [
    "aiohttp>=3.11.18",
    "pymodbus>=3.9.2",
    "pytest>=8.3.5",
    "grpcio==1.65.1",
    "protobuf==5.28.2",
    "pytest-asyncio>=0.26.0",
]
